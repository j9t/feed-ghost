name: "Update feeds"

on:
  schedule:
    # Weekly on Mondays at 09:00 UTC
    # Reference for changes: https://crontab.guru/
    - cron: '0 9 * * 1'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-feeds:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: "Install dependencies"
        run: pip install -r requirements.txt
      - name: "Generate feeds"
        run: python generate-feeds.py
      - name: "Commit changes"
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if [ -d feeds/ ]; then git add feeds/; fi
          if [ -f generate-feeds.log ]; then git add generate-feeds.log; fi
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Update feeds [$(date -u '+%Y-%m-%d')]"
            git pull --rebase
            git push
          fi